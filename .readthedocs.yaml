version: 2
build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
  jobs:
    install:
      - pip install uv==0.12.13
      - uv sync --locked --group docs
    build:
      html:
        - uv run --group docs python -m mkdocs build --strict --site-dir "$READTHEDOCS_OUTPUT/html"
